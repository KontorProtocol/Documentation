[package]
name = "pool-test"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[lints.clippy]
too_many_arguments = "allow"

[dependencies]
testlib = { workspace = true }
stdlib = { workspace = true }

token-test = { path = "../../token/test" }
